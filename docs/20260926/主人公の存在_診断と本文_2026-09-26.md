# 主人公の存在 — 診断と本文（2026-09-26）

**文書のみ。`src/` は未編集。** 作者指摘「主人公の存在がストーリーに薄すぎる」への回答。
まず実装から「主人公」を確定し、薄さの正体を数え、足すだけで成立する本文を出す。

---

## 0. 主人公は誰か（実装からの確定）

| 候補 | 実装上の根拠 | 判定 |
|---|---|---|
| **あなた（ご主人様）＝プレイヤー** | `Hud.cs:418` `LineKind.Boy = 0`、`Hud.cs:467/502` 話者ラベル **「あなた」**、`Hud.cs:500` コメント「Boy＝プレイヤー本人（案C に少年は居ない）」。`wiki/08_仮台本/12:5`「案C（少年なし、ご主人様＝あなた …）」。`Prologue.cs:10`「案Cでは少年は登場しない」。who=0 の行は全部 **ChoiceOverlay で選んだ下書きの復唱**（`StageAkari.cs:171` 等）。 | **主人公（プレイヤーの器）はこちら。** 「少年」は旧正典の名残で、現行の who=0 は顔も名前も声も持たない「あなた」 |
| ミナ | 語り手（memory narration-rule）・自機・感情獲得の背骨（memory mina-emotion-arc）・FINAL は彼女の内側。 | **劇的な意味の主役はミナ。** ただしミナは十分に濃い（各面 60〜80 行）。「薄い」と言われているのはミナではない |

結論: 作者の指摘は **who=0「あなた」** に向いていると読む。以下「少年」＝「あなた（ご主人様）」。

> 補足: 「主人公＝ミナ」と読む根拠は上の通り存在するが、その場合の処方（ミナの独白増量）は既に飽和している。本書は「あなた」を扱う。

---

## 1. 現状の診断

### 1.1 シーンごとの who=0 行数と、少年への言及

「復唱」＝選んだ下書きをそのまま (0, "…", "") で流す行。**1周で実際に出るのは各選択で最大1行**（（送らない）なら0行）。

| シーン（ファイル） | who=0 定義数 | 1周で出る最大 | 「ご主人様」呼びかけ | うち“人として”向いた行 | ボス→少年の認識 |
|---|---|---|---|---|---|
| PROLOGUE `Prologue.cs` | 復唱 P2/P3/P4 | 2〜3 | 3（`:196,205,279`） | 0（命名の相談は業務） | — |
| STAGE1 道中 `StageAkari.cs` | 8（s1_4×2, s1_5×3, s1_2×3） | 3 | 7（`:75,99,154,156,234,275,289`） | 2（`:234`「ご主人様は。——雨。」／`:289` 空の問い①） | **0**（カメオの「あなた」`:134` はミナ宛） |
| STAGE1 改心 `BossAkari.cs` | 0 | 0 | **0** | 0 | 0 |
| STAGE2 道中 `StageKoharu.cs` | 9（s2_1/s2_2/s2_4 各3） | 3 | 6（`:61,165,206,250,310,356`） | 2（`:250`「ご主人様の画面です」／`:356` 空の問い②） | △ カメオ第一声が s2_1 の言葉を引用（`:121-135`）。ただし言葉はミナのものとして扱う |
| STAGE2 改心 `BossKoharu.cs` | 0 | 0 | **0** | 0 | 0 |
| STAGE3 道中 `StageRei.cs` | 8（s3_2×3, s3_5c×3, s3_7×2） | 3 | 6（`:82,189,223,265,318,331`） | 3（`:223`「見えていますか」／`:311` 空の問い③／`:318` 涙の報告） | △ カメオ第一声が s3_2 を引用「数えたの、**あなた**なの?」`:145`＝ミナ宛 |
| STAGE3 改心 `BossRei.cs` | 0 | 0 | **0** | 0 | 0（「あなたが書いて」`:132` はレイ自身） |
| FINAL 導入 `StageMina.cs` | 0 | 0 | 1（`:39`）＋「あなた」3（`:62,64,66`） | 3（「あなたの光」） | — |
| FINAL 段間 `MinaPhaseScene.cs` | **11（脚本済み・非選択）** `:72-90` → `:231` で Boy 表示 | 11 | 1（`:113`） | 1 | — |
| FINAL 邂逅 `BossMina.cs` | 0 | 0 | 0 | 0 | レイ「知ってる。あんたの声だった。」`:96`＝ミナ宛 |
| F4 `Final.cs` | 「あなた」1（【初】） | 1 | 2（`:104,105`） | 2 | — |
| EPILOGUE `Epilogue.cs` | E6 0〜1 | 1 | 10（コメント含む） | 4（`:198,215,221,266`） | 三人は少年に触れない（`:207-217`） |
| HUB `Hub.cs` | 小話 **13（脚本済み）** `:3041-3124` | 抽選1本/入場 | 23 | 5（猫背 `:3095`・句点 `:3122`・食べ物 `:3069` 等） | 返信 H1r「あなたの言い方、誰かに似てる。」`:3307`＝ミナ宛 |

**合計（1周・選択式）: 復唱 ≤14 行、全て 12 文字以下。** 脚本済みの「あなた」は FINAL 段間 11 行＋ハブ小話（抽選）のみ。

### 1.2 少年の選択が物語に効いている箇所（`ChosenAt` 等の読み出し）

| 選択 id | 記録 | 読まれている先 | 備考 |
|---|---|---|---|
| p2 | 最初の言葉 | `Prologue.cs:195-196`（迷い秒・文字数）／**F4 の【初】** `Final.cs:117`（FirstScattered）／E6 対句 `Epilogue.cs:244-250`（HesitationAt） | 最も長距離で効く |
| p3 | 命名 | E6 nameEcho `Epilogue.cs:256-261`（NameRoute） | |
| p4 | だれの声／見なかったことにする | **【激情】初期値のみ** `FuryMeter.cs:74-79` | 物語上の反響なし |
| s1_5 | 中ボス・ね? | ハブ返信に1行 `Hub.cs:3282-3289`／激情 | |
| s1_2 | 雨 | 再訪小話「雨粒」固定＋1行 `Hub.cs:3259-3268`／激情 | |
| s1_4 | 十二件 | 激情のみ `FuryMeter.cs:81-86`（（送らない）で【濁】+0.02） | 物語上の反響なし |
| s2_1 | 予定表 | **カメオ第一声を差し替え** `StageKoharu.cs:139-145`／激情 | 2026-09-25 先例 |
| s2_2 | ペンライト | スコア +500 `StageKoharu.cs:693-695`／激情（2件は 0） | |
| s2_4 | カーソル | 激情のみ。**「F4 の悲鳴の枠」は未実装**（`Final.cs` に参照なし。`ChoiceEffects.cs:44-48` が宙吊り） | 宙吊り |
| s3_2 | 見えていますか | カメオ第一声 `StageRei.cs:161-167`／再訪小話「同接」`Hub.cs:3269-3275`／激情 | |
| s3_5c | 三つの席 | 激情のみ。**「E4 の開示」は存在しない**（`ChoiceEffects.cs:14` のコメントが宙吊り） | 宙吊り |
| s3_7 | つづけて | F1 の1行 `StageMina.cs:48-58`（S37Quote）／（送らない）で【濁】 | 先例 |
| f4 | 【初】 | LastSentWord → スタッフロール `Epilogue.cs:73,135` | |
| e6 | また来る／ありがとう | 対句のみ。以後なし | |

【激情】は第1段（値と表示のみ、`FuryMeter.cs:14-19`）で、道中の選択が弾幕・台詞・結末に効く段はまだ無い。つまり **12 か所中 4 か所（p4/s1_4/s2_4/s3_5c）は、いま物語のどこにも返ってこない。**

### 1.3 「薄い」の正体（具体）

1. **三つの山場に少年がいない。** 改心の `Lines`（`BossAkari.cs:98-108` / `BossKoharu.cs:148-166` / `BossRei.cs:125-135`）は who=0 が 0 行、「ご主人様」も 0 回。決定打はミナ単独（案Cの決定「証人型」。意図どおりだが、少年は三人の物語の頂点に一度も居合わせない）。
2. **ヒロインが少年を認識しない。** ボス・カメオの「あなた」は全部ミナ宛。少年の言葉を引用する 2026-09-25 のカメオ差分（s2_1/s3_2）ですら、言葉の主をミナだと思っている（「数えたの、あなたなの?」）。世界の側から見ると、少年は存在していない。
3. **少年の存在形式が二つしかない。** (a) 12 文字以下の復唱行、(b) ミナの報告の宛先「ご主人様」。(b) の大半は「ご主人様、これ。」「下書きが、開いています。」＝オペレータへの事務連絡で、人として向いた行は各面 2〜3 行（空の問い、雨、見えていますか、ご主人様の画面）。
4. **少年の言葉は受け1行で消費され、下流は各1行。** その1行も「ハブの小話」「返信」「カメオ」「F1」に散っていて、ヒロインの物語の中では響かない。4 か所は下流が無い（1.2）。
5. **中核テーマの四人目が薄い。** memory core-theme-drafts「対象は三人＋プレイヤー自身」。プレイヤー自身の掘り起こしは F4 で【初】が1語戻ることに圧縮されている。S2-4「ご主人様の画面です」は良い仕込みだが、その後どこにも続かない。
6. **少年の「手」が読まれない。** 「足取りは、ご主人様のままで」（`Hub.cs:3185`、`CompanionDialogue.cs:48`）＝避ける・撃つ・止まる手は少年のもの、が正典なのに、ボスは手を見ない。読まれるのは被弾数（ハブ `Hub.cs:3199,3222`）と迷い秒（P2/E6）だけ。
7. **逆に一番濃い場所がルール違反。** FINAL 段間の `MinaPhaseScene.cs:72-90` は「あなた」が選んでいない台詞を 11 行喋る（「ひとりで、ここまで抱えていたんだね。」等）。案C の「あなたの台詞＝下書き選択式」（memory theme-priority-heroines）に反し、少年に脚本の声が付いてしまっている。存在の“濃さ”がここだけ突然変わる。

**既にある良い装置（壊さず、ここに足す）**: 空の問い×3（未回答のまま）／迷い秒（P2, E6）／被弾 {n} 回／猫背・句点・打ち間違い（ハブ小話）／S2-4「ご主人様の画面」／H1r「あなたの言い方、誰かに似てる。」／F3「あんたの声だった。」／起動ログ `import unsent_drafts ... 414`（`Prologue.cs:145`）と F4「四百十四件」（`Final.cs:103`）／「足取りは、ご主人様のままで」。

とくに **「ミナの声はあなたの未送信 414 件から合成された」**（`Prologue.cs:145-146`、memory theme-priority-heroines「ミナはあなたの未送信414件から生まれた」）は、少年が **ミナの言い回しそのものとして物語の中に居る** ことを意味する。だがこの事実は H1r と F3 の各1行でしか触れられず、三人の物語の中で“気づかれる”場面が無い。ここが最大の未回収。

---

## 2. 方針（4案・推奨1つ）

| 案 | 中身 | 長所 | 短所 |
|---|---|---|---|
| **A. ミナが返す** | 選択の後・道中・ボス戦中に、ミナが少年の言葉や癖に反応する行を足す | 最安。ミナの語りの延長で書ける | 少年は「ミナに話しかけられる人」のまま。呼びかけは既に 60 回超あり、量ではなく質の問題。ボス戦中にミナが喋る経路が S3-7 以外に無い |
| **B. ヒロインが気づく「ミナの向こうの誰か」** ★推奨 | 中ボス・ボス戦中・改心後に、三人が **自分の闇の器官**（あかり＝読む／こはる＝手／レイ＝数える・見る）でミナの向こうの少年を感知する。言い回し（＝414 件の声）と手（＝足取り）の二経路 | 少年に顔・名前・経歴を足さずに「世界から観測される側」になる。三人の物語を削らず、三人自身の主題で書ける。H1r→F3 の既存伏線（12 設定シートの「気づかれたかった人が、最初に気づく人になる」`wiki/08_仮台本/12:204`）が三面を貫く線になる。**12 に書かれて未実装の「あんたの言い方、この人に、そっくりよ」を拾える** | 置き場所を誤ると P4（ヒロインを少年の器にする）。→ 決定打ブロックには一行も入れず、カメオ／RECLOSE／挑発／クリア後／E5b に限定する |
| **C. 少年の下書き** | ミナが一度だけ少年自身の下書きを読む | テーマの四人目に正面から応える | 本文を書いた瞬間に少年の内面が確定する。`docs/20260914/ストーリー添削:535`「作中で明かした瞬間にあなたの内面が確定する」、`12:208`「あなたの側の設定は空欄のまま増やさない」に正面衝突。安全に出来るのは「散った言葉（少年が自分で捨てた語）を読み返す」だけで、これは docs/20260924 の END C 案と重なる |
| D. 手＝プレイの痕跡を読む | 迷い秒・手が止まる・避け方を、ボスが喋る | 少年の実在の証拠が“操作”そのもの＝同一化を壊さない | 単独では小さい。B の一経路として吸収する |

### 推奨: **B（＋A を糊として2行、D を3行）**

理由:
- 薄さの正体は「誰にも見られていない」（1.3-2）と「山場に居ない」（1.3-1）。A は前者を解決しない。B は三人の目で少年を立てるので、**最優先テーマ（三人の闇）を強めながら**少年が立つ。
- 少年に事実を一つも足さない。三人が感知するのは **言い回し**（ミナの声の出所＝少年の 414 件）と **手**（足取り）で、どちらも既に正典。プレイヤー＝少年の同一化は壊れない。
- 既存台詞を一行も削らず、配列への追加だけで成立する（4 章）。
- 三人の感知は別々の器官（読む／手／数える）なので、同型反復に見えない（maeda-pitfalls P1）。到達点は全員「後ろに、誰か」で、語彙は少しずつ違える（三人は互いに他人＝`12:11`）。

---

## 3. 推奨案の本文（足す行だけ・全 17 行）

書式: `(int who, string text, string face)`。who: 0=あなた／1=ミナ／2=相手ボス／4=投稿。
挿入位置は **その行の直後**。既存行は一字も変えない。face の定数名は各ファイルのもの。

### 3.0 PROLOGUE — 仕込み（1行）

`Prologue.cs:197`（`L(WhoMina, "……敬っている、とは言っていませんが。", FMina)`）の直後、P2Reply 内:

```csharp
L(WhoMina, "……いまの言い回し。……どこで覚えたのか、記録に、ありません。", FMina),
```

> 皮肉の言い回し（sarcasm.dll 200%）の出所が自分の記録に無い＝414 件の声（あなた）から来ている、の最初の仕込み。ミナは観測しか言わない（起動時の感情ゼロ規約に抵触しない）。H1r「誰かに似てる」→ S1 クリア「聞いたことある」→ F3「そっくりよ」→ F4「四百十四件」で回収される。

### 3.1 STAGE1 あかり — 「読む人」が、読んでいるもう一人に気づく（5行＋差分2）

**(a) 中ボス第一声の p4 差分（2行・どちらか1行が出る）** — `StageAkari.cs:145`（CameoPost）の直後に配列を追加し、`StageKoharu.cs:139-145` と同じ `CameoIntroFor(game)` を新設して `:745` `IntroLines = CameoTalk1` を差し替える。p4 は（送らない）が無いので旧セーブ以外は必ずどちらかに落ちる（無ければ既存 CameoTalk1）。

```csharp
// 「だれの声」
(2, "あ、来た来た。……「だれの声」って、探したでしょ。……あたしのは? あたしの声は、探してくれないの? ね?", AFaceLit),
// 「見なかったことにする」
(2, "あ、来た来た。……「見なかったことにする」って、したでしょ。……あたしのも、そうするの? 見たよね? 見たって言って。ね?", AFaceLit),
```

> 冒頭で少年が下した「知ろうとした／見ないと決めた」が、一面目のヒロイン（既読と返事の人）に真っ先に返る。2026-09-25 のカメオ差分と同じ「会う前から聞かれていた」の作法。「見て」は第一声の中に留める（12 の但し書きどおり）。

**(b) ボス戦 RECLOSE（1行）** — `BossAkari.cs:494`（`"バカ。……バカ、バカ。"`）の直後、index 2 に挿入（最終形「返して」は最後のまま）:

```csharp
"……読んでるの、あなただけじゃ、ないでしょ。……後ろの人。ねえ、そっちも、返して。",
```

> 読まれることに飢えた人が、画面の前で読んでいるもう一人を見つけて、返事を求める。少年はここで返事の手段を持たない＝あかりの主題（返って来ない）が少年に直接向く。

**(c) クリア後（3行。うち1行は迷い秒ゲート）** — `StageAkari.cs:284`（`"……あったかい声が、した。……知らない声なのに。変なの。"`）の直後:

```csharp
(2, "……ねえ。あのとき、すぐ決めなかったでしょ。……うん。それで、いい。", AFace),          // ★s1_4 で迷ったときだけ（4章）
(2, "……知らない声のくせに。……言い方だけ、どこかで、聞いたことある。", AFace),
(1, "……言い方は、わたくしのです。……たぶん。", MWorried),
```

> 「たぶん」がミナの声の出所に入る最初のひび。説明しない。H1r「誰かに似てる」の直前に置かれるので、ハブの返信が答え合わせではなく二段目になる。MWorried はあかり面で使用済みの顔（感情アーク内）。

### 3.2 STAGE2 こはる — 「打って消す手」の人が、手を見る（3行）

**(a) ボス戦 RECLOSE（1行）** — `BossKoharu.cs:685`（`"やめないで……止まったら——"`）の直後、index 2 に挿入（取り繕い「楽しいってば」は最後のまま）:

```csharp
"……ねえ。いま動いてるの、そっちの手じゃ、ないでしょ。……あたし、手は、見るもん。",
```

> 「みんな見てる」と言い張る子が、本当に見ているのは手だけ（自分が打って消す手を見続けてきた人）。自機の足取りは少年のもの（正典）。

**(b) クリア後（2行。うち1行は迷い秒ゲート）** — `StageKoharu.cs:355`（`"入力欄に、一行、増えました。……"`）の直後:

```csharp
(2, "……迷ってくれたよね。あたし、それ、見てたよ。", KFace),                          // ★s2_4 で迷ったときだけ
(2, "……あの手。……ペンライト、ちゃんと振ってた。……点いてなくても。", KFace),
```

> 捨て台詞「振ってみて。楽しいから。」（`:111`）の日常語の再来。振っていたのは少年の手（撃つ＝振る）。点かないペンライトを振り続けた少年を、こはるが肯定する。

### 3.3 STAGE3 レイ — 「数える人」が、数に入らないミナの向こうを数える（5行）

**(a) ボス戦・挑発（1行）** — `BossRei.cs:76`（`"戦ってよ。わたしを、ちゃんと見てよ。"`）の直後、TauntLines の 3 本目:

```csharp
"……手、止まってる。……見てるだけの人、ひとり、増えたのね。",   // 笑顔のまま。ガワは割らない
```

> 「また逃げる」圧（与ダメゼロ 6 秒）で出る行なので、**少年の手が止まったとき**にしか出ない＝文字どおり少年の手を見ている。同接が 1 増える理屈（S3-9「4」）の予告。

**(b) ボス戦 RECLOSE（1行）** — `BossRei.cs:487`（`"初見さん、まだ、いてくれるでしょう?"`）の直後、index 2 に挿入:

```csharp
"……あんたの後ろ。初見さん、いるでしょう? ……いらっしゃい。",
```

**(c) クリア後（3行。うち1行は迷い秒ゲート）** — `StageRei.cs:309`（`"右上の数字が、「4」に。……ひとつ、増えました。"`）の直後:

```csharp
(2, "……四。……あんたは、数に入らないんでしょう。……なら、増えたの、誰。", RFace),
(1, "……集計は、向こう側の、ものですので。", MFace),
(2, "……即答されてたら、たぶん、信じてなかった。", RFace),                             // ★s3_5c で迷ったときだけ
```

> S3-2 の受け「こちらは、向こう側の数に、入りませんので」（`:241`）を反転する。ミナは画面の内側だから同接に入らない。入るのは画面の外＝少年。「誰が増えたかは言わない」（`:303` のコメント）は守る＝レイが問い、ミナが答えない。気づくのはプレイヤー。

### 3.4 FINAL F3 — 三面後、レイが言い切る（1行）

`BossMina.cs:96`（`(2, "知ってる。あんたの声だった。", "res://char/v3/rei_face.png")`）の直後:

```csharp
(2, "……あんたの言い方。……この人に、そっくりよ。", "res://char/v3/rei_face.png"),
```

> **12 設定シート（承認済み）`:191,204` に書かれていて未実装の行。** 「この人」＝自機の「あなたの光」（`StageMina.cs:62-66`）。一面目にあかりが言いかけたこと（H1r）を、気づかれたかった人が三面あとに言い切る。ミナの次行「……助けてって。言っても、よかったのですね。」にそのまま繋がる。

### 3.5 EPILOGUE E5b — 三人のうち一人が、宛先を変える（1行）

`Epilogue.cs:210`（レイ `"一桁のほうが、顔が見えるの。……数えてるんでしょ。なら、胸張りなさいよ。"`）の直後、`:211` `"…………。"`（曲停止）の前。独自レンダラ形式:

```csharp
G("こはる", "……ミナの後ろの人も、聞いてるんでしょ。——ありがと、知らない人。");
```

> H2r「ありがと、知らない人。」（`Hub.cs:3314`、ミナ宛）の日常語の再来。同じ言葉のまま、宛先だけが少年に変わる（maeda-lines §5）。曲停止行は本文一致検索（`Epilogue.cs:379`）なので挿入で壊れない。

### 3.6 本文の総数と、自信のある3行

- 書く行: **17**（P0 1／S1 5＋差分1／S2 3／S3 5／F3 1／E5b 1）。1周で出る行: 13〜16（迷い秒ゲート 0〜3、カメオ差分は片方）。
- 三つの決定打ブロック（`Lines`）には **0 行**。ヒロインの物語は削っていない。

**特に自信のある3行**
1. F3 レイ「……あんたの言い方。……この人に、そっくりよ。」— 承認済み設定の未実装行。「この人」＝あなたの光。三面かけて気づく人が育つ線の着地。
2. E5b こはる「……ミナの後ろの人も、聞いてるんでしょ。——ありがと、知らない人。」— 同じ言葉で宛先だけ変わる。少年が“知らない人”のまま礼を受ける＝内面を確定させずに存在だけが立つ。
3. S1 クリア あかり「……知らない声のくせに。……言い方だけ、どこかで、聞いたことある。」＋ミナ「……言い方は、わたくしのです。……たぶん。」— ミナの声の出所に最初のひびが入る対句。説明ゼロで、H1r・F3・414 が全部この二行の続きになる。

（次点: レイの挑発「……手、止まってる。……見てるだけの人、ひとり、増えたのね。」— 少年の手が止まった瞬間にだけ出る、文字どおり少年を見ている行。）

### 3.7 自己レビュー（maeda-pitfalls ゲート）

- P1: 三人の感知器官が別（読む／手／数える）。到達語も「後ろの人」「そっちの手」「あんたの後ろ」「ミナの後ろの人」と揃えすぎない。
- P2: 少年に事実を足さず、少年の **選択（p4/s1_4/s2_4/s3_5c）と手（止まる・迷う）** だけを世界が読む。能動の痕跡を素材にしている。
- P3: 「足取りはご主人様」「ミナは同接に入らない」「声は 414 件から合成」の三つの既存ルールにだけ乗る。新設定なし。
- P4 ★: 決定打ブロック無改変。三人の行は全部、自分の主題（返事／手／数）の延長。少年を慰める行は一つも無い（こはるの「振ってた」も、ペンライトの話）。
- P5: who=3 なし。感情語直書きなし。ミナの内心説明なし（「たぶん」「向こう側のものですので」で止める）。
- P6: 建てた伏線（P0）は F3/F4 で既に回収先がある。新規の伏線は建てていない。
- P7: 全て既存配列・既存経路（RECLOSE／TauntLines／Clear／`Lines`／`G()`）。秒単位演出への依存なし。

---

## 4. 実装の当たり（未実装・読むだけで確認した範囲）

| 追加 | 入れる配列／経路 | once／再生 | 分岐（ChosenAt 等） | 注意 |
|---|---|---|---|---|
| P0 | `Prologue.P2Reply()` `:185-200` の List | 周回ごとに毎回（既存と同じ） | なし | 承認済み本文への1行追加。起動時のミナは観測のみの規約内 |
| S1(a) カメオ差分 | 新配列 `CameoTalk1_P4Ask` / `CameoTalk1_P4Skip` ＋ `CameoIntroFor(game)` を新設し `StageAkari.cs:745` の `IntroLines` へ | カメオが出るたび（AfterMidBoss 入口では出ない＝既存どおり） | `game.ChosenAt("p4")` を `StageKoharu.cs:139-145` と同型で switch。空なら `CameoTalk1` | p4 に（送らない）は無い |
| S1(b) S2(a) S3(b) RECLOSE | `RecloseLines[]`（`BossAkari.cs:491-496` / `BossKoharu.cs:682-687` / `BossRei.cs:484-489`）の index 2 に挿入 | サイクル順送り。`Min(idx, len-1)` で **最後の行が以後ずっと出る** ので、最終形を最後に残すため index 2 | なし | Easy（HP2本）は RECLOSE 回数が少なく index 2 まで届かない可能性。挑発（S3(a)）が保険になる |
| S3(a) 挑発 | `BossRei.TauntLines[]` `:73-77` の 3 本目 | 与ダメゼロ 6s→+1, 以後 4s ごと。CD 8s。3本目以降は同じ行が繰り返される | なし | 「手が止まった」条件と台詞が一致する |
| S1(c) S2(b) S3(c) クリア後 | `Clear[]`（`StageAkari.cs:281-291` / `StageKoharu.cs:347-358` / `StageRei.cs:306-321`） | クリアのたび（既存と同じ） | **迷い秒ゲートの3行**だけ条件付き（下記） | あかりは `ClearBefore = Clear.Take(2)` `:293` がフィルム前の行数をベタ書き → 挿入位置がフィルム前なので、迷い秒ゲートで行数が 4/5 に揺れる。`Take(n)` ではなく「`……♥が、ひとつ。` の行（`:288`）の手前までをフィルム前」と本文一致で割るのが安全。他ジョブ潜行（`_charStory`）では Clear を使わないので影響なし |
| F3 | `BossMina.Lines[]` `:93-100` | 毎回 | なし | FINAL はジョブ不問でミナ本編固定（`StageMina.cs:77-80`） |
| E5b | `Epilogue._gaze` `:196-218` に `G("こはる", …)` | 毎回 | なし | `SilenceLine` は本文一致（`:379`）で安全。EdgeFor("こはる") 既存 `:589` |

**迷い秒ゲート（3行）の当たり**: `GameManager.HesitationAt(id)`（`GameManager.cs:570`）を読む純関数で、Clear を実行時に組む（`StageKoharu.Step_MidEndLines` `:536-553` が `static readonly` の写しを作る先例）。閾値は docs/20260924 §2.4 の `HesitationAt(id) > HesitationAt("p2")`（最初の選択より長く迷った）を採るか、固定秒（例 8s。沈黙の自動決定は 20s）にするかは作者判断。id: あかり=`s1_4`／こはる=`s2_4`／レイ=`s3_5c`。`HasChoiceAt(id)` が false（ボスから入場）なら出さない。

**新しい永続項目は不要**（すべて `_chosenById` / `_hesitationById` の既存台帳）。

---

## 5. 既存設定と矛盾しそうで迷った点

1. **あかりカメオが p4 を知っている根拠。** P4 はあかりの部屋の外での選択。こはる／レイ差分の「会う前から聞かれていた」慣例に乗せたが、一面目で初めてその慣例が出るぶん唐突に見える恐れ。落としても他は成立する。
2. **レイ Clear「4」の正体。** 設計コメントは「誰が増えたかは言わない」。私の行は問いの形にしたが事実上少年を指す。根拠にした「ミナは同接に入らない」（`StageRei.cs:241`）の読みが作者の意図と違えば、S3(c) の2行を落とす。
3. **「手」の帰属。** STAGE1〜3 の自機はミナの身体。手＝ご主人様は `Hub.cs:3185`・`CompanionDialogue.cs:48` の説明行に依拠した。作者が「手もミナ」と考えるなら、こはる S2(a)(b) とレイ S3(a) は落とす。
4. **「後ろの人」の共有。** 三人は互いに他人（`12:11`）。語彙を少しずつ変えたが、三人が同じ結論に至ること自体を作者が嫌う可能性はある。
5. **迷い秒の3行**は docs/20260924（未承認）§2.4 からの借用。文言は本書で微調整。閾値未定。
6. **MinaPhaseScene の「あなた」11行**（1.3-7）は本書の範囲外だが、主人公の一貫性のために「ご主人様（Tank）のときは Companion 行を出さない／ミナ独白へ置換」の判断が要る。未編集・提案のみ。
7. **宙吊りの効果 2 件**（s2_4→F4 悲鳴、s3_5c→E4）。存在の薄さの一因。本書では触れていない。
8. **A 案を採らなかった件**: ボス戦中にミナが喋る経路は S3-7（`StageRei.cs:329-356`）しか無く、「ミナが少年の癖に反応する in-fight 行」を足すには新経路が要る。文書だけで済む範囲を優先した。

以上。`src/` は未編集。
