# DEV_QUEUE — 自動開発キュー

> **このファイルが自動開発の唯一の正本。** 夜間クラウド routine が上から順に1タスクずつ消化する。
> 人間（指揮官セッション）は「積む・並べ替える・仕様を書く」だけ。実装は routine が回す。
>
> 詳細な背景は `docs/タスク管理.md` / `docs/ゲーム開発ロードマップ.md` を参照。

## 運用ルール（routine 用）

1. `## TODO` の**最上段の1タスクだけ**を取る。複数着手しない。
2. 着手したら状態を `WIP` にして先にコミット（多重着手の防止）。
2.5. **実装前に「もう実装済みでないか」を `src/` の grep と git log で確かめる。** 済んでいたら実装せず DONE へ根拠付きで移す（`docs/タスク管理.md` の状態欄は古いことがある。実コードが正）。
3. 完了したら `## DONE` の先頭へ日付付きで移動。
4. 実装が15分以上かかる／仕様が曖昧なら、着手せず `## BLOCKED` へ理由付きで移し、次のタスクへ進む。
5. 必ずビルドを通してからコミットする（`dotnet build algo_shoot.sln`）。通らなければ変更を捨てて BLOCKED 送り。
6. **1タスク = 1コミット。** 1回の実行では時間の許す限り複数タスクを順に消化する（1〜6を繰り返す）。ただし複数タスクを1コミットにまとめない。
7. **時間予算は3時間**（3:00開始→6:00まで）。各タスクの着手前に残り時間を確認し、**残り20分を切ったら新しいタスクを始めない**。ビルド失敗/BLOCKEDが3連続でも打ち切る。

### タスク記法

```
- [ ] (P1) #22 既読スキップ | engineer | 既読フラグをセーブに持たせ、既読会話を高速送り可に
```
`(P1)` 優先度 / `#22` タスク管理.md の番号 / `engineer` 担当ワーカー / `|` 以降が受入条件。

---

## TODO

<!-- ▼ 2026-08-13 4観点監査（game-designer/engineer/scenario/qa 並列）で洗い出した修正候補。上から実害順 -->

<!-- ▼ 2026-08-13 「新奥義バランス査定」タスクの副産物として発見。要ユーザー判断（影響範囲が武器強化ツリー全体） -->
- [ ] (P1) パネル持ち敵にダメージ強化が一切効いていない | game-designer→engineer | `Panel.cs:103` のインク削り（`Ink--`）が `Bullet.Damage` を一切参照しない。結果、`shot_power`/`rapid_power`/`spread_power`/`homing_power`/`focus_fire` 系の威力強化は**雑魚・バズ壁・ボスSHIELDEDフェーズのパネルには何の効果もなく**、実効範囲は「ボス無防備窓」（`Enemy.cs:468` で `Mathf.Clamp(b.Damage,1,4)` に頭打ち）だけに限定されている。ショップの武器強化ツリーの大部分が体感と乖離した無駄買いになっている可能性大。**要ユーザー判断**：(a)パネルもダメージ量に応じてインクを削るよう`Panel.cs`を修正（雑魚の耐久が実質下がる＝難易度再調整が要る）、(b)露出窓のダメージクランプ(1〜4)を緩めて強化の実感先をボス戦に一本化する方針を明文化、のいずれかを選んでから着手すること

- [ ] (P1) M5 全ステージ×全難易度QA一巡 | qa | 当たり判定・進行不能・例外・弾リーク・FPS を全ステージ横断で。発見した不具合は本キューの TODO 先頭に積む（修正は別タスク）
- [ ] (P2) M5 設計書との乖離最終チェック | qa | design-gap で乖離レポートを更新し、意図的な差分かを仕分け。要対応分は TODO に積む
- [ ] (P2) タスク管理.md の状態を実装に合わせる | engineer | #22/#27/#11 は実装済みなのに管理表が ☐/◐ のまま。実コードを正として状態欄と補足を更新（このズレが自動開発の空振りを生む）
- [ ] (P3) #15 ボスラッシュ制約 | engineer | ボスラッシュを回復なし・ボム復活なしに。**モード本体が未実装**（`src/` に BossRush の実装なし）なので、まずモードの有無を確認し、無ければ実装規模を見て BLOCKED 判断してよい
- [ ] (P3) 未発注イラスト | artist | ミナ詠唱ポーズ、スマイル差分、少年スピンフレームを生成・配線（レイ/あかりの cry は配線済みなので対象外）

## WIP

（なし）

## BLOCKED

- [ ] 排他フォーク/振り直し一式が到達不能な死にコード | engineer | 単Lvノード方式への移行で全60+ノードの `ExclusiveWith` が全て空になり、`RespecRefund`/`RespecFee`/`TryRespec`/`TotalPaid` と `Shop.RespecStep`/`DrawRespecPanel`（約100行）/`_respecArmed` 状態機械/`sealed_` 分岐が全て到達不能。タスク文面自体が「削除するか、あるなら注記するか」を**要ユーザー判断**と明記しており、復活予定の有無は実コード調査だけでは分からない仕様判断のため実装せず据え置き
- [ ] グレイズを教えるチュートリアルが無い | game-designer | StageZero は移動/撃つ/低速/回避/ボム/浄化/やさしさ全開の7項目を達成条件つきで教えるのに、グレイズだけ独立ステップが無い（`StageZero.cs:123` で一言触れるのみ）。低速(phase5-6)と回避(7-8)の間に枠を1つ挿入、達成条件は `GameManager.GrazeCount` の増分で5回――という指定だが、`StageZero.cs` の `_phase` は0始まりの生の`int`で `switch(_phase)`（本体）と `OpForPhase(phase)`（HUD操作ヒント）の**2箇所に同じ番号がハードコードされて一対一対応**しており（`case 7`〜`case 15` が9ブロック）、中間に挿入するには両方のswitchでcase 7以降を全て+2で振り直す必要がある。約480行のステートマシンで達成条件のフラグ（`_t7ActivatedT`等）も番号に紐付いており、番号ズレは実行時のみ症状が出て**ビルドでは検出できない**（このコンテナはインタラクティブなプレイテストができない）。加えて「厳密にphase 6と7の間へ挿入」と「末尾へ追加（教える順序が変わるが安全）」のどちらを取るかは設計判断で、勝手に決めていいものではないと判断。人間の判断待ち：(a)番号振り直しでの中間挿入を許可する（要動作確認）、(b)安全側で末尾に追加する、(c)phase識別をenum化してから着手する別タスクに分割する、のいずれか指定してほしい

- [ ] ショップ画面の毎フレーム再描画を実測してから判断 | qa→engineer | `Shop.cs:430` が入力・アニメの有無に関わらず毎フレーム `QueueRedraw()` し、`_Draw` が60+ノード・エッジ・詳細パネルを全部辿って `DrawColoredPolygon`/`DrawArc` を大量発行する。**過去にホーミング弾の毎フレーム再描画で85fps→9fpsに崩落した実績と同じ構図**（`Bullet.cs:372-376` のコメントに記録あり）。ただし静止画面なので体感問題が出ていない可能性が高く、**実測していないので断定できない**。同型は `Hub.cs:259-269`/`DiffSelect.cs:206`/`Settings.cs:159`/`Records.cs:69`/`Credits.cs:153`。まず `--qa` でFPSを実測し、**落ちていなければ触らない**（動いているものを闇雲に直さない）。落ちていればアニメ中フラグ化→静的層の分離の順で対処
- [ ] ウィンドウリサイズ/フルスクリーン切替の表示確認 | qa | `--shot` は固定解像度でビューポートを保存するため**自動では検証できない**。手動でウィンドウサイズを変えて各画面のレイアウト崩れを目視する必要がある。**ユーザーの実機確認待ち**
- [ ] 死亡系フロー（残機0・コンティニュー・チェックポイント再開）のQA | qa | 直近のQA走行は `--assist`（無敵）だったため死亡経路を通っていない。god無しで実際に死ぬパスを作って、ポーズ→リトライ／ステージ途中離脱／コンティニューの状態復帰を確認する必要がある。上の「死亡時チェックポイント再開」タスクを実装する場合はその検証も兼ねる
- [ ] 世界観の肉付け（SNSの雑音で語る） | scenario | 設定書は `docs/世界観設定_v1.md`（未作成・要執筆）に集約予定。言葉弾の語彙を面ごとに分ける／流れるツイート増量／Hub帰還小話に設定の断片／雑魚の名前を呼ばせる。**言葉弾の文面をユーザーが調整したいとのことで判断待ち**（レイ=数字と序列 / あかり=未送信と時間 / こはる=停止の促し、の方向は合意済み）
- [ ] マルチエンディング化 | scenario+engineer | 提案済み・**ユーザーの方式判断待ち**。本命は「Epilogue の PW ゲート（`_unlocked` が実装済みで未使用のデッドコード）を本物の分岐にして 真相ルート/沈黙ルート の2種」。必須の付帯条件＝**エンディングだけ見直す導線**（タイトルに「回想」を足して Epilogue へ直行）が無いなら着手しない。腕前（ノーミス/難易度）での分岐は両ワーカーが明確に否定（Easy勢が永久に真相へ届かないため）
- [ ] 挿入歌の調達 | — | 完全一致するフリー素材なし。インスト続投 or 委託の**ユーザー判断待ち**
- [ ] BGM権利文言の再確認 | — | 2026-07-30 の Google ToS 改定後に `BGM/acquisition_list.md` §5 の文言を見直す（現行10曲はフリー素材差し替え済みのため影響は小）
- [ ] 旧Geminiマスター9本の削除 | — | `BGM/` に残置中。削除するかは**ユーザー判断待ち**
- [ ] 人力確認: R長押しリトライ / ESC の操作感 | — | 自動では判定不能。**ユーザーの実プレイ待ち**

## DONE

<!-- routine がここに追記する。新しいものが上 -->
- [x] (P1) 新奥義バランス査定 | game-designer | (完了 2026-08-13) 集中の光＝据え置き（原因は対象外の`Enemy.cs`/`Panel.cs`側、根拠コメント追記）。連鎖の光＝射程無制限が強すぎたため`ChainRange=170px`を追加。バズ壁＝耐久は妥当だが報酬(points)が手間に見合っていなかったため150→220。副産物として「パネル持ち敵にダメージ強化が無効」という大きめの発見をTODO先頭に起票。※Player.cs編集で既存フィールド`_focusTarget`/`_focusHits`が誤って消えビルド不能になっていたため指揮官側で復元・ビルド確認済み
- [x] (P2) 小話5: タイトル画面のひとこと | engineer | (完了 2026-08-13) `docs/小話集_v1.md` §5を`TitleMenu.cs`へ実装。既存`Ticker`は無変更。起動時1回＋無操作10秒ごとに1回、画面下中央帯（x≈290-990）にミナ色40%不透明度で薄く表示。Hotspot登録なし＝メニュー操作に無関係
- [x] (P2) 小話4: トレーニング場の独り言 | engineer | (完了 2026-08-13) `docs/小話集_v1.md` §4を`TrainingRoot.cs`へ実装。DPS計器直下(x430-774,y224-320)にトースト器を新設。入場時1回、命中ごとに10%抽選（表示中は再抽選しない）、無入力7秒で放置トーク（入力復帰まで連呼しない）
- [x] (P2) 小話3: ショップの一言 | engineer | (完了 2026-08-13) `docs/小話集_v1.md` §3を`Shop.cs`へ実装。既存の`Toast()`機構を流用（新規UI不要と判明）。入店時1回、購入時25%で確認メッセージの代わりに一言、退店は「一言→0.8秒遅延→実遷移」の`_exitPending`ガード付き。オートプレイ(`--qa`)でのデッドロックを避けるよう判定順を配慮
- [x] (P1) 小話2: ハブの複数パターン化 | engineer | (完了 2026-08-13) `docs/小話集_v1.md` §1のIdleDialogs/SmallTalksを`Hub.cs`へ実装。クリア直後は既存`ReturnDialog`のまま、それ以外の入場は50%抽選で`TryStartIdleSmallTalk()`が未読優先で1本再生（全既読ならリセット）。既読は`GameManager`に`HashSet<string>`で永続化（Save/Load/ResetPersistent対応、後方互換ガード済み） | scenario | (完了 2026-08-13) `docs/小話集_v1.md` §2 のChat1〜5をStageRei/Akari/Koharuに実装。ステップ番号は無変更、既存の`Mid`/`BossTalk`/`MidEnd`配列に`.Concat().ToArray()`で追記する形で組み込み、既存台詞は無変更
- [x] (P3) FINALの投稿弾がミナ自身の言葉になっていない | scenario | (完了 2026-08-13) `StageMina.cs:201-209` PostWordsを三人の語の断片（レイ「追いつけない」/あかり「すき、すき」/こはる「もう帰ってこない」）＋ミナ自身の語（「わたくしの、せいです」「ご主人様」「……アホですね」）へ混成
- [x] (P3) Epilogueの死因回収が1行で長く二段落としが潰れている | scenario | (完了 2026-08-13) `Epilogue.cs:149` の1行を「——」の前で2つの`O("地",...)`呼び出しに分割。テキスト内容・語順は無変更
- [x] (P3) `TrainingDummy.HpRatio` が基底を隠している | engineer | (完了 2026-08-13) `Enemy.cs:75` の `HpRatio` を `virtual`化、`TrainingDummy.cs:78` を `override`化。フルリビルドで **0 Warning / 0 Error** を確認（ソリューション全体の警告が今回でゼロに）
- [x] (P3) CS8602警告4件を解消してゼロ警告にする | engineer | (完了 2026-08-13) `StageRei/Akari/Koharu/Mina.cs` の `Hud?.SetElapsed(...)` を `Hud.SetElapsed(...)` へ統一（4ファイル）。フルリビルドでCS8602ゼロ件を確認、残る警告はCS0108（別タスク）のみ
- [x] (P3) 未使用カウンタとコメントの嘘 | engineer | (完了 2026-08-13) `GameManager.cs:110` の `GrazeCount` コメントから実在しない「チュートリアル検出に使う」を削除し、現状読み手なしの実態に即した記述へ修正。`DodgeGrazeCount`等は受入条件通りスコープ外のため未変更
- [x] (P3) リトライ長押し0.7sが反復プレイで重い | game-designer | (完了 2026-08-13) `RetryHold.cs:10` `HoldTime` を0.7f→0.45fへ短縮。充填チップは離せば即キャンセルされる構造のため誤爆防止は維持
- [x] (P3) ショップの「おすすめ」が所持状況を見ていない | game-designer | (完了 2026-08-13) `Shop.cs:126-175` `RecommendedNow()` を書き換え。所持済みid除外、`SelectedShotMode`対応系統の未所持入り口ノードを先頭差し込み、`CanPurchase()`で買える物優先の安定ソート、全所持済み時は前段階へフォールバック
- [x] (P3) 少年の二人称がFINALだけ揺れる | scenario | (完了 2026-08-13) `BossMina.cs:64`「お前に」→「きみに」。`:65`は感情の高ぶりとして現状維持（受入条件通り）。`StageMina.cs`の類似文言は対象外のため未変更
- [x] (P3) トレーニングのスキルタブが画面右端で見切れる | engineer | (完了 2026-08-13) `TrainingRoot.cs:319`の`tabX`計算を`Mathf.Min(PanelX-96f, W-96f-6f)`へ変更し、閉状態で画面右端から6pxの内側マージンを確保。開状態のパネル内位置には影響なし
- [x] (P3) ミナの「小さな願い」が1回きりで伏線にならない | scenario | (完了 2026-08-13) (a)(b)両方実装。`StageKoharu.cs:164-165`に軽い再来（まだ願っている、少年もまた流す）、`Epilogue.cs:164`に叶わないままの代償（「どなたかの、空の写真で」＝他人の投稿越しにしか空を知らない）を追加。願いが叶ったとは書いていない
- [x] (P3) Hub帰還会話がステージ側の段階設計を逆転させている | scenario | (完了 2026-08-13) `Hub.cs:955` の「……エスパーか何かで?」を「……お見事です。」へ変更。STAGE1 Clear（`StageRei.cs`）の「訝りゼロ＝上り坂の起点」より踏み込んだ疑いをHub側が出す逆転を解消
- [x] (P3) こはるの「怒り＝他責」が一度も描かれない | scenario | (完了 2026-08-13) `StageKoharu.cs:104-113` の `CameoTalk3` に無邪気→他責の怒り（KPale・「あの人のせいだ」名は伏せる）→また無邪気（KFace）の裏返り一往復を追加。`BossKoharu.cs`の中継行に先立つ伏線として機能
- [x] (P2) 難易度の賭け金と見返りが釣り合っていない | game-designer | (完了 2026-08-13) `GameManager.cs:665` `DifficultyImpressionMulFor` の Hard 1.4→1.6、Lunatic 2.2→3.0。Easy/Normal・負荷パラメータ（弾数/弾速/HPバー/残機）は無変更
- [x] (P2) FINAL導入が4行でクライマックスの助走が無い | scenario | (完了 2026-08-13) `StageMina.cs` Introを4行→10行へ拡張。who=4（投稿）でレイ/あかり/こはるの声の残響を挿み、`Final.cs`の「レイの。あかりの。こはるの。」の予感を作った（文字列は意図的に非一致＝DropLine検出に無影響）。骨格（無音バナー→壊れた声→決意）と既存4行は不変
- [x] (P2) シェイクスピア引用の三段仕掛けが二段しか無い | scenario | (完了 2026-08-13) `StageRei.cs:166-167`に1回目「"All the world's a stage."」（無害な衒学、少年の軽口＋ミナの茶化しで意味を持たせない）を追加。`Epilogue.cs:159-162`に3回目「"To thine own self be true."」（死因・時期の回収直後、日本語訳を付けずミナの一言だけで皮肉を示す）を追加。2回目（`StageAkari.cs:166`）と`Final.cs`の既存引用は無変更
- [x] (P2) あそびかた・バックログがマウス非対応 | engineer | (完了 2026-08-13) `HowToPlay.cs`/`Backlog.cs` に `Pad.MouseRightClick()`（閉じる）と `Pad.WheelDelta()`（HowToPlayはページ送り、Backlogは縦スクロール）を追加。DiffSelect/Records/Credits/Shopと同じ規約に合わせた
- [x] (P2) 後方弾の形と色の指定が無視されている | engineer | (完了 2026-08-13) (a)案。`Bullet.cs` に `DrawPlayerDiamond` を追加し `!IsEnemy` switch に `case Diamond` を接続。淡い金 `BackMid(0.98,0.86,0.55)` の進行方向尖り菱形で敵弾/他3モードと明確に区別。`Player.cs` の `FireBackfire` tint も同色へ揃えた
- [x] (P2) 死亡時にチェックポイントから再開できない | game-designer | (完了 2026-08-13) `ReiRoot.cs`/`AkariRoot.cs`/`KoharuRoot.cs` の game-over 中 R 即発リトライに Shift 分岐を追加。R単体＝`SelectedEntry=Boss`をセットしてreload（各`_step`＝Rei:10/Akari:11/Koharu:9）、Shift+R＝従来通り最初から。`--boss`デバッグ時のDebugAlwaysBoss持ち回りを壊さないようShift時はSelectedEntryへ触れない設計。MinaRoot(FINAL)は道中構造が無く意味が同じになるため対象外（理由をコメントで明記）。`GameManager.cs`のgame-overプロンプト文言も更新。QA未実施（推奨: qa-autoplayで3面のR/Shift+R挙動確認）
- [x] (P2) ステージ途中でハブへ戻れない | game-designer | (完了 2026-08-13) `PauseMenu.cs` の `ItemsJp` に「ハブへもどる」を「タイトルへ」の直前へ追加（`HubReturnAction = SlotCount+4`）。`RetryEnabled` と同条件でステージ外はグレーアウト、Z/クリック2段階確認（1段目は移動音のみで確定させない、ラベルが「ほんとうに もどる？」に変化）を経て `AutoSave()→Pool.DespawnAll()→Hub.tscn` を実行
- [x] (P2) 汚染ゲージの説明が実装と食い違う | game-designer | (完了 2026-08-13) (a)案。`HowToPlay.cs:222,264` の説明文を実態（ステージ進行連動の固定演出、選択で変わらない）に合わせて書き換え。「救うほど濁る」「表裏一体」というトレードオフ誤解表現を削除、「澄んだ心I/II」は上昇を緩める効果だけ明記。ロジックは無変更
- [x] (P1) Hubリプライがエピローグの二段落としを先に割る | scenario | (完了 2026-08-13) `Hub.cs:1003` の @akari 台詞から「庇って」「来なくなった」を削除し声の類似（伏線③）のみ残す形に改稿。`Epilogue.cs:145,148-149` で初めて死因を再構成する二段落とし演出が先に割れなくなった
- [x] (P1) ミナが汚染を引き受ける選択の一行が無い | scenario | (完了 2026-08-13) `StageKoharu.cs:162-164` の `Clear` 末尾に3行追加。「息が詰まる」「重さ」の身体感覚のみで示し（汚染/ゲージ等の語は不使用）、「わたくしが、持ちます」でミナ自身の能動の意志として描いた。監査報告P2-4の文面案は実体が見当たらず、maeda skillで新規に書き起こし。face(`mina_doubt`/`mina_worried`/`mina_smile`)は既存アセットのみ使用、`Final.cs` 側との接続確認は別タスク送り
- [x] (P1) 通常グレイズが通貨を生まない | game-designer | (完了 2026-08-13) `AddGraze()`（`GameManager.cs:1118`）に `GainImpression(1)` を追加。回避グレイズ(+2)より少額に留め優位性は保持。farming対策は `Player.OnGrazeAreaEntered` の `_hitInvincible` スキップと1弾1回課金（`Grazed`フラグ）で既存担保
- [x] (P1) ボス戦が火力投資に反応しない | game-designer | (完了 2026-08-13) (b)案採用。`Enemy.cs` の `TickBossPhase`（`BossPhase.Exposed` 分岐）で `_windowDamage >= ExposedDamageCap` を検知したら `_phaseT >= VulnDur` を待たず即 `EnterReclose()` へ遷移するよう変更。火力に投資するほど窓4.0sをフルに使わず早くキャップに到達し、次のBREAKサイクルへ速く進む。弱火力時は従来通り4.0秒フルに残るので体験は不変。(a)案「1本＋超過分50%持ち越し」は別タスク送り
- [x] (P1) マウス未操作なのに選択が勝手に飛ぶ | engineer | (完了 2026-08-14) `Pad.cs` に `_mouseSeeded` を追加し、初回 `PollMouse` は `_mousePosPrev` を実座標に揃えるだけにして移動判定をスキップ。初期値(0,0)から跳ぶぶんでの誤ラッチが消えた。QAスクショで Hub の初期選択が t=0.3/t=3.0 とも解放済みの「レイ」で固定を実測（修正前はカーソル位置次第でロック中カードへ移動）
- [x] (P1) 「はじめから」の初期化漏れ2件 | engineer | (完了 2026-08-14) `ResetPersistent()` に `Contamination = 0f;` と `ClearTimes.Clear();` を追加。新規データが前ランの汚染1.0とベストタイムを継承しなくなった
- [x] (P1) SelectedEntry が消費後リセットされずリトライが壊れる | engineer | (完了 2026-08-14) Rei/Akari/Koharu の `_Ready` で入口を読んだ**直後**に消費（switch で `_step` を決めた後なので入口選択は無視されない）。副作用として `--boss` が2面目以降効かなくなるため `GameManager.DebugAlwaysBoss` を新設して貼り直す。QA実測で `--boss` 起動時に Rei/Akari/Koharu すべて `boss=1.00` 開始を確認（走破 708s→346s が道中スキップの裏付け）
- [x] (P1) #22 既読スキップ | engineer | (完了 2026-07-05) 実装済みだったものを誤ってキューに積んでいた。`GameManager` の `IsLineRead`/`MarkLineRead`＋`user://read.json`、Ctrl/RB で既読行のみ高速送り
- [x] (P2) #27 ステージ選択のSNSタイムライン化 | engineer | (完了 済) `Hub.cs` は投稿カード・X風タブ・縦フィードで実装済み
- [x] (P2) #11 背景の文字をツイート風に | engineer | (完了 済) `StageImagery.cs` がツイート風カードの流れるレイヤーとして実装済み
- [x] (P3) レイ/あかりの cry ポートレート | artist | (完了 済) 画像・配線とも完了（`BossRei.cs` / `BossAkari.cs`）
