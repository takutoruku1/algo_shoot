# DEV_QUEUE — 自動開発キュー

> **このファイルが自動開発の唯一の正本。** 夜間クラウド routine が上から順に1タスクずつ消化する。
> 人間（指揮官セッション）は「積む・並べ替える・仕様を書く」だけ。実装は routine が回す。
>
> 詳細な背景は `docs/タスク管理.md` / `docs/ゲーム開発ロードマップ.md` を参照。

## 運用ルール（routine 用）

1. `## TODO` の**最上段の1タスクだけ**を取る。複数着手しない。
2. 着手したら状態を `WIP` にして先にコミット（多重着手の防止）。
3. 完了したら `## DONE` の先頭へ日付付きで移動。
4. 実装が15分以上かかる／仕様が曖昧なら、着手せず `## BLOCKED` へ理由付きで移し、次のタスクへ進む。
5. 必ずビルドを通してからコミットする（`dotnet build algo_shoot.sln`）。通らなければ変更を捨てて BLOCKED 送り。
6. 1実行 = 1タスク = 1コミット。終わったら PROGRESS を更新して終了。

### タスク記法

```
- [ ] (P1) #22 既読スキップ | engineer | 既読フラグをセーブに持たせ、既読会話を高速送り可に
```
`(P1)` 優先度 / `#22` タスク管理.md の番号 / `engineer` 担当ワーカー / `|` 以降が受入条件。

---

## TODO

- [ ] (P1) BGM試聴チェック | qa | 全10スロットをゲーム内で鳴らし、シーンと合っていないスロットを列挙。合わないものは `BGM/candidates.md` の次点と交換を提案（差し替え実作業は別タスク）
- [ ] (P2) #27 ステージ選択のSNSタイムライン化 | engineer | `Hub.cs` を「Xの川」の世界観へ。ステージ選択がタイムラインの投稿として並ぶ見た目に
- [ ] (P2) #11 背景の文字をツイート風に | engineer | `Background.cs` / `GlyphMote` の流れる文字を投稿風の短文に。文面は scenario ワーカーに書かせる
- [ ] (P2) 新奥義バランス査定 | game-designer | 集中の光（8ヒット/段）・連鎖の光（×0.4）・バズ壁の硬さを実装値で査定し、数値を具体調整
- [ ] (P3) #15 ボスラッシュ制約 | engineer | ボスラッシュを回復なし・ボム復活なしに。低優先、他が詰まっていなければ
- [ ] (P3) 未発注イラスト | artist | レイ/あかりの cry ポートレート、ミナ詠唱ポーズ、スマイル差分、少年スピンフレームを生成・配線
- [ ] (P3) M5 全ステージ×全難易度QA一巡 | qa | 当たり判定・進行不能・例外・弾リーク・FPS を全ステージ横断で。発見した不具合は本キューの TODO 先頭に積む
- [ ] (P3) M5 設計書との乖離最終チェック | qa | design-gap で乖離レポートを更新し、意図的な差分かを仕分け

## WIP

（なし）

## BLOCKED

- [ ] 挿入歌の調達 | — | 完全一致するフリー素材なし。インスト続投 or 委託の**ユーザー判断待ち**
- [ ] BGM権利文言の再確認 | — | 2026-07-30 の Google ToS 改定後に `BGM/acquisition_list.md` §5 の文言を見直す（現行10曲はフリー素材差し替え済みのため影響は小）
- [ ] 旧Geminiマスター9本の削除 | — | `BGM/` に残置中。削除するかは**ユーザー判断待ち**
- [ ] 人力確認: R長押しリトライ / ESC の操作感 | — | 自動では判定不能。**ユーザーの実プレイ待ち**

## DONE

- [x] (完了 2026-08-13) #22 既読スキップ | engineer | 調査の結果 2026-07-05 のコミット `fcc09d5` で既に実装済み（`GameManager.cs` の `read.json` 永続化 + `Hud.cs`/各 Stage の `FastForwarding`）と判明。新規コード変更なし。DEV_QUEUE の記載が `docs/タスク管理.md` との同期漏れによる古い重複だったため、正本に合わせて DONE 送りにした。

<!-- routine がここに追記する。新しいものが上 -->
